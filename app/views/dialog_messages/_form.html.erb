<%= form_for ( @message  ) do |mess| %>
  <% if @message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@message.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @message.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= mess.label :message %><br>
    <%= mess.text_area :message %>
  </div>
  <div class="actions">
    <%= mess.submit %>
  </div>
  <%= hidden_field_tag :dialog_id, @dialog.id %>

<%end%>